# Dependencies for pdf-extraction-tests/comparison.ipynb
# These are research/prototype deps — not required for the production app.
# The production app deps live in app/requirements.txt.
#
# Install into the project venv:
#   pip install -r pdf-extraction-tests/requirements.txt

# PDF extraction tools under evaluation
docling==2.76.0
magic-pdf[full]==1.3.12

# MinerU runtime dependencies
opencv-python-headless>=4.12.0.88,<5.0.0.0
pycocotools>=2.0.7
